---
---
%ul.unstyled
  - sorted_articles.each do |article|
    %li
      %article
        %span
          = pretty_date(article)
        &raquo; &nbsp;
        %a{:href => url_for(article) }
          = "#{article[:title]}"
