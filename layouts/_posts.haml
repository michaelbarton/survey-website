---
---
%ul.unstyled
  - sorted_by_kind(kind).each do |article|
    %li
      %article
        %span
          = pretty_date(article)
        &raquo; &nbsp;
        %a{:href => relative_path_to(article) }
          = "#{article[:title]}"
